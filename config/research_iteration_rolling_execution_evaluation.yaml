iteration:
  name: iteration_026_rolling_execution_evaluation
  output_dir: reports/research_iterations/iteration_026_rolling_execution_evaluation

source_policy_config: config/research_iteration_microstructure_order_policy_017.yaml
infrastructure_config: config/infrastructure_microstructure.yaml

data:
  ws_depth_dir: data/microstructure/ws_depth
  symbol: BTCUSDT
  default_days: 7

windows:
  rolling_hours: [24, 168, 720]

verdict:
  min_collection_hours: 24
  max_invalid_snapshot_fraction: 0.05
  max_p90_slippage_bps: 1.0
  max_entry_skipped_rate: 0.15
  adjusted_monthly_floor: 0.13
  required_equity_ceiling: 10000
  size_cap_probe: 25000
